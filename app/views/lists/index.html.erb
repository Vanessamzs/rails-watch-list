<h1>Lists#index</h1>
<ul>
  <% @lists.each do |list| %>
    <li><%= list.name%></li>
    <%= link_to 'Add bookmark', new_list_bookmark_path(list) %>
  <% end %>
</ul>
<%= link_to "Add a new list", new_list_path %>
